=========================================
T3CKGROUP TASK MONITORING - FINAL SPRINT
=========================================

[ ] GLOBAL TEXT & STATE CHANGES (Quick Wins)
    - Rename database/UI status: "REJECTED" -> "NOT APPROVED".
    - Update all dropdown filters to say "Not Approved".

[ ] FIXING THE "NOT APPROVED" (REJECTED) VISIBILITY
    - Bug: "Not Approved" tasks are currently hard to find.
    - Fix: Ensure the Task Directory and Approvals inbox actually fetch and display tasks with the status "NOT APPROVED". (Check the activeTasks filter in the useMemo hooks to ensure we didn't accidentally scrub them out alongside "DELETED" tasks).

[ ] TASK CREATION / EDITING
    - Disable the "Start Date/Time" input for EVERYONE (Heads, HR, and Employees). Lock it to system-generated timestamps or read-only.

[ ] UI/UX: TAGS & LABELS
    - Org Stats Dashboard: Add a "Verified / Unverified" visual tag/badge on tasks for HR's view.
    - /tasks Directory: Add the exact same "Verified / Unverified" tag so HR can spot them while scrolling.
    - Task Details Drawer: When a task is evaluated, explicitly display the Name and Role of the Head/Manager who graded it.

[ ] NEW HEAD CAPABILITIES
    - Feature: Department Heads can now explicitly Reject ("Not Approve") a task.
    - UI: Add a required "Head Remarks" text input that appears when they click "Not Approve" (Mirroring the HR rejection flow).
    - Database: Ensure `remarks` and `status = 'NOT APPROVED'` are saved correctly by the Head.

[ ] HR DASHBOARD ENHANCEMENTS
    - /hr-master-log: Add the robust Filter Bar (Date, Status, Priority, Dept, Sub-Dept, Employee) mirroring the /tasks page.
    - "Employee Deep-Dive" View: Add a specific stats section/filter in the HR Dashboard to isolate and monitor a single employee's total pipeline (Completion %, Average Grade, Pending Count).

[ ] THEMING (The Final Boss)
    - Implement Light Mode / Dark Mode toggle.
    - (Note: Tailwind's `dark:` classes or a CSS variables strategy will be needed here).

=========================================



can access other routes (but empty)
    - should kick out and go to login